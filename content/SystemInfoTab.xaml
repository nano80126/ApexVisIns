<StackPanel x:Class="MCAJawIns.content.SystemInfoTab" 
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:local="clr-namespace:MCAJawIns.content" 
            xmlns:sys="clr-namespace:System;assembly=mscorlib"
            xmlns:ctrl="clr-namespace:MCAJawIns.Control" 
            
            mc:Ignorable="d" 
            d:DesignHeight="900" 
            d:DesignWidth="1600" 
            UseLayoutRounding="False" 
            Orientation="Vertical"
            Loaded="StackPanel_Loaded" 
            Unloaded="StackPanel_Unloaded">
    <!--#region StackPanel Resources -->
    <!--#endregion-->

    <Grid Style="{StaticResource TabGrid}" Background="{StaticResource ColorInfoTransparent}">
        <!--#region Layout -->
        <!--#endregion-->

        <ctrl:CustomCard HorizontalAlignment="Center" VerticalAlignment="Center" Title="系統資訊" IconKind="InformationOutline" MinWidth="800">
            <Grid MinHeight="300">
                <!--#region Resources -->
                <Grid.Resources>
                    <Style TargetType="{x:Type TextBlock}">
                        <!--<Setter Property="FontSize" Value="{StaticResource BasicFont}" />-->
                        <Setter Property="HorizontalAlignment" Value="Right" />
                        <Setter Property="VerticalAlignment" Value="Center" />
                        <Setter Property="Margin" Value="0 0 12 0"/>
                        <!--<Setter Property="Background" Value="{StaticResource ColorInfoTransparent}" />-->
                    </Style>
                    <!--//-->
                    <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
                        <Setter Property="HorizontalAlignment" Value="Center" />
                        <Setter Property="VerticalAlignment" Value="Center" />
                        <Setter Property="Margin" Value="0 8" />
                        <Setter Property="Width" Value="120" />
                        <Setter Property="TextAlignment" Value="Center" />
                        <Setter Property="IsReadOnly" Value="True" />
                        <Setter Property="Focusable" Value="False" />

                        <!--<Setter Property="BorderThickness" Value="0" />-->
                    </Style>
                </Grid.Resources>
                <!--#endregion-->

                <!--#region Layout -->
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="1*" />
                    <!--<ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="1*" />-->
                </Grid.ColumnDefinitions>
                <!--#endregion-->

                <!--<Grid Grid.Column="0" Background="{StaticResource ColorAccentTransparent}"></Grid>-->

                <!--#region System info -->
                <UniformGrid HorizontalAlignment="Stretch" VerticalAlignment="Top" Columns="2">
                    <TextBlock Text="作業系統" />
                    <TextBox Padding="0 3" />

                    <TextBlock Text="平台" />
                    <TextBox Padding="0 3" />

                    <TextBlock Text="PID" />
                    <TextBox Padding="0 3" />

                    <TextBlock Text=".NET" />
                    <TextBox Padding="0 3" />

                    <TextBlock Text="資料庫" />
                    <TextBox Padding="0 3"  Text="123"/>

                    <TextBlock Text="系統時間" />
                    <TextBox Padding="0 3" Text="{Binding ActualWidth, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=UniformGrid}, Mode=OneWay}" />
                    <!--<TextBox Padding="0 3" Text="123" />-->
                </UniformGrid>
                <!--#endregion-->

                <!-- Separator -->
                <Separator Grid.Column="1" Grid.Row="100" Margin="8 0" Width="1" Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}"
                        Background="{StaticResource ColorGrayTransparent}" />
                

                <!--#region NewRegion -->
                <UniformGrid Grid.Column="2" HorizontalAlignment="Stretch" VerticalAlignment="Top" Columns="2">
                    <TextBlock Text="版本" />
                    <TextBox Padding="0 3" />

                    <TextBlock Text="當前狀態" />
                    <TextBox Padding="0 3" />

                    <TextBlock Text="自動模式運作時間" />
                    <TextBox Padding="0 3" />

                    <TextBlock Text="自動模式運作時間" />
                    <TextBox Padding="0 3" />

                    <TextBlock Text="資料庫" />
                    <TextBox Padding="0 3" Text="123" />

                    <TextBlock Text="系統時間" />
                    <TextBox Padding="0 3" Text="{Binding ActualWidth, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=UniformGrid}, Mode=OneWay}" />
                </UniformGrid>
                <!--#endregion-->

                <!-- Separator -->
                <!--<Separator Grid.Column="3" Grid.Row="100" Margin="8 0" Width="1" Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}"
                        Background="{StaticResource ColorGrayTransparent}" />

                <Grid Grid.Column="4" Background="{StaticResource ColorSuccessTransparent}"></Grid>-->

            </Grid>
        </ctrl:CustomCard>
    </Grid>
</StackPanel>
