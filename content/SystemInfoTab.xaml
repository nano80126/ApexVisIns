<StackPanel x:Class="MCAJawIns.content.SystemInfoTab"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:local="clr-namespace:MCAJawIns.content"
            xmlns:sys="clr-namespace:System;assembly=mscorlib"
            xmlns:ctrl="clr-namespace:MCAJawIns.Control"
            
            mc:Ignorable="d" 
            d:DesignHeight="900" 
            d:DesignWidth="1600"
            UseLayoutRounding="True"
            Orientation="Vertical"
            Loaded="StackPanel_Loaded"
            Unloaded="StackPanel_Unloaded">
    <!--#region StackPanel Resources -->
    <!--#endregion-->

    <Grid Style="{StaticResource TabGrid}" Background="{StaticResource ColorInfoTransparent}">
        <!--<Grid Style=""></Grid>-->

        <!--#region Layout -->
        <!--#endregion-->

        <TextBlock Text="123" />

        <ctrl:CustomCard HorizontalAlignment="Stretch" VerticalAlignment="Center" Title="系統資訊" IconKind="InformationOutline">
            <Grid MinHeight="300">
                <!--#region Resources -->
                <Grid.Style>
                    <Style TargetType="{x:Type TextBlock}">
                        
                        
                    </Style>
                </Grid.Style>
                <!--#endregion-->

                <!--#region Layout -->
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>
                <!--#endregion-->

                <!--<Grid Grid.Column="0" Background="{StaticResource ColorAccentTransparent}"></Grid>-->
                <UniformGrid HorizontalAlignment="Stretch" VerticalAlignment="Top" Columns="2" MinHeight="30" Background="{StaticResource ColorAccentTransparent}">

                    <TextBlock HorizontalAlignment="Right">1</TextBlock>

                    <TextBlock HorizontalAlignment="Center">2</TextBlock>

                    <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center">3</TextBlock>

                    <TextBox VerticalAlignment="Center" HorizontalAlignment="Center" Padding="0 3" MinWidth="80" Style="{StaticResource MaterialDesignOutlinedTextBox}" />
                </UniformGrid>
                
                <Separator Grid.Column="1" Grid.Row="100" Margin="8 0" Width="1" Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}"
                        Background="{StaticResource ColorGrayTransparent}" />

                <Grid Grid.Column="2" Background="{StaticResource ColorBrownTransparent}"></Grid>

                <Separator Grid.Column="3" Grid.Row="100" Margin="8 0" Width="1" Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}"
                        Background="{StaticResource ColorGrayTransparent}" />

                <Grid Grid.Column="4" Background="{StaticResource ColorSuccessTransparent}"></Grid>

            </Grid>
        </ctrl:CustomCard>
    </Grid>
</StackPanel>
