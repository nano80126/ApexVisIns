<StackPanel x:Class="ApexVisIns.control.SpecListView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:local="clr-namespace:ApexVisIns.control"
            xmlns:pd="clr-namespace:ApexVisIns.Product"
            mc:Ignorable="d" 
            d:DesignHeight="450" d:DesignWidth="800" 
            DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
            d:DataContext="{d:DesignInstance Type={x:Type local:SpecListView}}"
            Loaded="StackPanel_Loaded">
    
    <StackPanel x:Name="Header" Orientation="Horizontal" Background="{StaticResource ColorErrorTransparent}">
        <StackPanel.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Padding" Value="8 3"/>
                <Setter Property="Background" Value="{StaticResource ColorSuccess}"/>

            </Style>
        </StackPanel.Resources>

        <TextBlock x:Name="col1" Text="1"></TextBlock>
        <TextBlock x:Name="col2" Text="1"></TextBlock>
        <TextBlock x:Name="col3" Text="1"></TextBlock>
        <TextBlock x:Name="col4" Text="1"></TextBlock>
        <TextBlock x:Name="col5" Text="1"></TextBlock>
        <TextBlock x:Name="col6" Text="1"></TextBlock>
    </StackPanel>

    <ScrollViewer VerticalScrollBarVisibility="Visible" Style="{Binding ScrollViewerStyle}"
                  Height="200" 
                  Background="Red">
        <ItemsControl ItemsSource="{Binding ItemsSource}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Left" Background="{StaticResource ColorInfo}"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                    <Setter Property="MinHeight" Value="32"/>
                </Style>
            </ItemsControl.ItemContainerStyle>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" d:DataContext="{d:DesignInstance Type={x:Type pd:SpecBase}}">
                        <StackPanel.Resources>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="HorizontalAlignment" Value="Left"/>
                                <Setter Property="TextAlignment" Value="Left"/>
                                <Setter Property="MinWidth" Value="80"/>
                            </Style>
                        </StackPanel.Resources>

                        <TextBlock Background="Yellow" Text="{Binding Item}"/>
                        <TextBlock Text="{Binding CenterLine}"/>
                        <TextBlock Background="{StaticResource ColorInfo}" Text="{Binding LowerCtrlLimit}"/>
                        <TextBlock Text="{Binding UpperCtrlLimit}"/>
                        <TextBlock Background="{StaticResource ColorSuccess}" Text="{Binding Result}"/>
                        <TextBlock Text="{Binding OK}"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

    </ScrollViewer>
</StackPanel>
